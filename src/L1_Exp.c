#include "../include/minishell.h"
/*
void ft_expans(char *str, char **env)
{
    char *new;
    int i;
    int size;

    i = 0;
    str = ft_strjoin(str, "=");
    size = ft_strlen(str);
    while (env[i] || !ft_strncmp(str, env[i], size))
        i++;
    if (env[i])
        new = ft_strdup(env[i]);
    return (new);
}
*/